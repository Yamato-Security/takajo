# Git 클론

다음 명령으로 저장소를 git clone하고 소스 코드에서 바이너리를 컴파일할 수 있습니다:

> 경고: 저장소의 main 브랜치는 개발 목적이므로 아직 공식적으로 릴리스되지 않은 새로운 기능에 접근할 수 있지만, 버그가 있을 수 있으므로 불안정한 것으로 간주하십시오.

`git clone https://github.com/Yamato-Security/takajo.git`
