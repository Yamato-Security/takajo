# Clonación con Git

Puedes clonar el repositorio con Git usando el siguiente comando y compilar el binario a partir del código fuente:

> Advertencia: La rama main del repositorio tiene fines de desarrollo, por lo que es posible que accedas a nuevas funciones que aún no se han publicado oficialmente; sin embargo, puede haber errores, así que considérala inestable.

`git clone https://github.com/Yamato-Security/takajo.git`
